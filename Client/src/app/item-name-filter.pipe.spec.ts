import { ItemNameFilterPipe } from './item-name-filter.pipe';

describe('ItemNameFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ItemNameFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
